{
  "name": "j",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "format": "prettier --write index.ts 'src/*.ts' 'test/*.js'",
    "test": "tsc --outDir .test-build --module commonjs --noEmit false && jest --runInBand",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check index.ts 'src/*.ts' 'test/*.js'",
    "test:live": "tsc --outDir .test-build --module commonjs --noEmit false && node test/live.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "cheerio": "^1.0.0-rc.12",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "typescript": "^5.1.3",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "itty-router": "^3.0.12"
  }
}
