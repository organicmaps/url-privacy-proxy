{
  "name": "j",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish",
    "start": "wrangler dev",
    "format": "npx prettier --write 'index.ts' && npx prettier --write 'src/coordinates.ts' && npx prettier --write 'test/main.test.js'",
    "test": "jest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "cheerio": "^1.0.0-rc.12",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "typescript": "^5.1.3",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "itty-router": "^3.0.12"
  }
}
